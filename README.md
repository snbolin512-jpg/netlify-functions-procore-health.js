# Ωboy V0.21.3 Clean Luxe UI Shell

This build replaces the stacked legacy front-end with one clean stable UI shell.

It fixes:
- blank page after load
- legacy full-width Cockpit bar
- overlapping cockpit generations
- duplicated patch scripts fighting each other

It preserves:
- Netlify functions
- Procore health/mock/webhook endpoints
- Jackson asset
- financial source map
- command modules
- drawing/schedule REV simulation flow
