{
  "name": "ohmboy-v020-full-restore-clean-storage",
  "version": "0.20.1",
  "private": true,
  "description": "Full OhmBoy UI restore with clean storage, cockpit layout recovery, button recovery, financial map tooltips, and white/copper/teal palette.",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "echo OhmBoy V0.20 full restore static build",
    "validate": "node ./scripts/validate-build.js"
  },
  "dependencies": {
    "@netlify/blobs": "^8.1.0"
  }
}