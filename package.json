{
  "name": "ohmboy",
  "version": "0.21.5",
  "private": true,
  "description": "OhmBoy static Netlify deployment with functions.",
  "type": "commonjs",
  "dependencies": {
    "@netlify/blobs": "^8.2.0"
  },
  "scripts": {
    "build": "node -e \"console.log('Static OhmBoy deploy: no build step required')\"",
    "validate": "node ./scripts/validate-build.js"
  }
}