{
  "name": "ohmboy-v020-full-restore-clean-storage",
  "version": "0.20.2",
  "private": true,
  "description": "Full OhmBoy UI restore with financial roll-up explainability, equation tooltips, ivory palette, and clean storage.",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "echo OhmBoy V0.20 full restore static build",
    "validate": "node ./scripts/validate-build.js"
  },
  "dependencies": {
    "@netlify/blobs": "^8.1.0"
  }
}