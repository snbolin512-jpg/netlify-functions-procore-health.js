{
  "name": "ohmboy-v020-full-restore-clean-storage",
  "version": "0.20.0",
  "private": true,
  "description": "Full OhmBoy UI restore from v0.18.11 with clean Netlify Blobs backend storage.",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "echo OhmBoy V0.20 full restore static build",
    "validate": "node ./scripts/validate-build.js"
  },
  "dependencies": {
    "@netlify/blobs": "^8.1.0"
  }
}