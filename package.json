{
  "name": "ohmboy-v020-full-restore-clean-storage",
  "version": "0.21.4",
  "private": true,
  "description": "Clean OhmBoy luxe UI shell with PM controls, Procore simulations, RFI/CO logs, and drawing takeoff page.",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "echo OhmBoy V0.20 full restore static build",
    "validate": "node ./scripts/validate-build.js"
  },
  "dependencies": {
    "@netlify/blobs": "^8.1.0"
  }
}