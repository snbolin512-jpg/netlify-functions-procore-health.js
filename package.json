{
  "name": "ohmboy-v020-full-restore-clean-storage",
  "version": "0.21.0",
  "private": true,
  "description": "OhmBoy full UI with luxe ivory/gold dashboard redesign, Jackson PM Daily Update, financial source map, and clean storage.",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "echo OhmBoy V0.20 full restore static build",
    "validate": "node ./scripts/validate-build.js"
  },
  "dependencies": {
    "@netlify/blobs": "^8.1.0"
  }
}