{
  "name": "ohmboy-v020-full-restore-clean-storage",
  "version": "0.21.3",
  "private": true,
  "description": "Clean OhmBoy luxe UI shell with no legacy stacked cockpit scripts.",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "echo OhmBoy V0.20 full restore static build",
    "validate": "node ./scripts/validate-build.js"
  },
  "dependencies": {
    "@netlify/blobs": "^8.1.0"
  }
}