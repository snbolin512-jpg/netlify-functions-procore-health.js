{
  "name": "ohmboy-clean-storage-rewrite",
  "version": "1.0.1",
  "private": true,
  "description": "Clean OhmBoy rewrite with durable Netlify Blobs storage. Build no longer fails on leftover repo files.",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "echo OhmBoy clean rewrite static build",
    "validate": "node ./scripts/validate-build.js"
  },
  "dependencies": {
    "@netlify/blobs": "^8.1.0"
  },
  "devDependencies": {}
}