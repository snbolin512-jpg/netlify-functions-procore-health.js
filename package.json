{
  "name": "ohmboy",
  "version": "0.21.6",
  "private": true,
  "description": "OhmBoy V0.21.6 Ops Linkage Audit with hardened Jackson asset, equation popovers, manual drawing/schedule intake, and feature map.",
  "type": "commonjs",
  "dependencies": {
    "@netlify/blobs": "^8.2.0"
  },
  "scripts": {
    "build": "node -e \"console.log('Static OhmBoy deploy: no build step required')\"",
    "validate": "node ./scripts/validate-build.js"
  }
}