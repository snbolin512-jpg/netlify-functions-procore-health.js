{
  "name": "ohmboy-clean-storage-rewrite",
  "version": "1.0.0",
  "private": true,
  "description": "Clean OhmBoy rewrite with Netlify Blobs durable storage and direct Netlify Functions.",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "node ./scripts/validate-build.js",
    "validate": "node ./scripts/validate-build.js"
  },
  "dependencies": {
    "@netlify/blobs": "^8.1.0"
  },
  "devDependencies": {}
}