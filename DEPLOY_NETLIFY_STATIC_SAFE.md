# Netlify Deploy

Use exactly:
Build command: [blank]
Publish directory: .
Functions directory: netlify/functions

Do not add a root package.json unless you intentionally want Netlify to initialize npm.
