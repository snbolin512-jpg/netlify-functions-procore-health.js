{
  "name": "Ωboy V0.22.0 — Ops Build",
  "version": "0.22.0",
  "base": "V0.21.6b Netlify Initialization Safe",
  "punch_list_completed": {
    "1_jackson": "Inlined as a base64 WebP data URI. No longer depends on assets/ reaching Netlify.",
    "2_takeoff_delete": "Per-row Delete plus Delete All, both confirmed. Deletions written to Audit Trail.",
    "3_packet_closeout": "Close Out panel on Packet Detail with disposition, closed-by, cost/schedule impact, and required resolution notes. Reopen supported. New Closed Packets register.",
    "4_report_intake": "Open PO and AR Aging upload (.xlsx/.csv). Feeds the Open PO Report, Aging Report, and Financial Source Map.",
    "5_manual_co_rfi": "Change Order Log and RFI Log are now PM manual-entry registers. Packet triage no longer writes to them; it flags exposure only. Optional source-packet link on each entry.",
    "6_schedule_intake": "Schedule upload (.xlsx/.csv). Re-upload diffs activity dates against the prior file and raises a packet for slipped activities.",
    "7_manpower_loading": "Bell-curve labor plan normalised to the estimate, with earned/actual overlay, cumulative S-curve, PF/CPI, peak crew, and forecast at complete. Timesheet import supported.",
    "8_drawing_intel": "PDF metadata + title-block/revision-block extraction. Raises a drawing packet flagged for PM review.",
    "9_procore_link": "Working OAuth2 start/callback/refresh, whoami, idempotent webhook hook+trigger registration, delivery log, and a hardened receiver."
  },
  "bugs_fixed": {
    "xlsx_export_corrupt": "Three separate defects made every exported .xlsx unopenable: double-escaped ZIP signatures (literal \\x03 text instead of byte 0x03), one extra u16 field in both the local file header and the central directory record, and charCodeAt&255 byte conversion that mangled non-ASCII. ZIP writer rewritten against APPNOTE using DataView + TextEncoder. Verified to open in a real spreadsheet reader.",
    "netlify_functions_500": "procore-auth-start, procore-auth-callback and procore-process-event imported json/parseJsonBody/normalizeProcoreLike/put/EVENT_PREFIX/uid/nowIso/storageStatus from _lib/store.js, which exported none of them. Every invocation threw. store.js rewritten to export them.",
    "xlsx_columns_past_z": "String.fromCharCode(65+c) produced invalid refs beyond column Z. Replaced with a base-26 column reference.",
    "xlsx_numbers_as_text": "All cells were written as inlineStr, so Excel could not sum or sort quantities. Numeric values now emit as numbers."
  },
  "dependencies": "None. XLSX reading uses the native DecompressionStream API; PDF reading is hand-rolled. No CDN, no npm install, no build step.",
  "browser_requirements": "DecompressionStream (Chrome/Edge 80+, Safari 16.4+, Firefox 113+). Required only for .xlsx and PDF uploads; the rest works everywhere."
}
